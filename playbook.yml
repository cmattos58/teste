- hosts: [terraform-ansible]
  tasks:
    - name: criar o aquivo 
      copy:
        dest: /home/ubunty/index.html
        content: <h1> Feito com terraform e ansible</h1>
    - name: criando o servidor
      shell: "nohup busybox httpd -f -p 8080 &"